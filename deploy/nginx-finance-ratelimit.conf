# FinanceMCP rate-limit zones. Installed to /etc/nginx/conf.d/ by deploy.sh.
limit_req_zone $binary_remote_addr zone=financemcp_auth:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=financemcp_mcp:10m rate=20r/s;
